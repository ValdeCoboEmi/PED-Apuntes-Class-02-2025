#include <iostream>

// declarar la funcion
int CaclularSuma(int x, int y);

//MAIN
int main()
{
   int x, y;
   int total;

   total = x + y;

   return 0;
}

//DEFINICION DE FUNCION
int CaclularSuma(int x, int y)
{
   int resultado;
   resultado = x + y;
   return resultado;
}